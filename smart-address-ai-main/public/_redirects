# SPA fallback: serve index.html for client-side routes (React Router).
# Netlify / some static hosts read this automatically.
# On Render Static Sites you must ALSO add a Dashboard rewrite (see GO_LIVE_RENDER_GODADDY_CLERK.md).
/*    /index.html   200
