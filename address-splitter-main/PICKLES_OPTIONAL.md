# Optional Pickles file (not in Git)

`Pickles/postcode_coords.pkl` is **large** and is **not** used by `parse_api.py` / the website parser. It was kept out of the Git repository so pushes to GitHub succeed under default security and size rules.

If you need it for legacy scripts under `Old Code/`, generate or restore it locally — do not commit it.
